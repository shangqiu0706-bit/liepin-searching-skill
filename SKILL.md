---
name: liepin-searching
description: Search and shortlist candidates on Liepin from a JD and hiring requirements, using keyword expansion, company reverse-search, evidence-based qualification, and structured delivery.
---

# 猎聘 Searching

当用户提供 JD、岗位要求或招聘筛选条件，并要求在猎聘上 searching、找人、筛候选人、扩搜或整理候选清单时使用本 skill。

## 目标

产出一份可复核的候选人池，而不是只返回关键词命中的简历：

- 先按 JD 和用户要求抽取关键条件、偏好、目标画像和薪资口径。
- 用“关键词搜索 + 公司反查”两条线并行发现候选人；发现新公司后，把公司加入反查队列。
- 公司发现方式必须由当前岗位的业务对象、工作内容、交付对象、渠道、组织关系和行业场景推导；甲方、乙方/DP/MCN、平台等只是可能的来源类型，不是每个岗位的固定搜索顺序。
- 以详情页证据完成最终判断；公司与职位名称是有效的发现信号，但不能单独推断能力。
- 输出推荐候选、可沟通边界、待验证线索和剔除原因，说明哪些条件尚未核实。

## 开始前

1. 若本机存在 Obsidian vault 的 `Codex/AGENTS.md`，先查看它及相关项目页；没有该记忆库时跳过此步。不保存联系方式、凭证或完整简历。
2. 识别本案：生成清晰的日期/项目目录，在其中维护 `case-config.md`、`search-log.md` 和 `outputs/`。若已有本案目录，先读取已有搜索词、公司队列、已查看简历 ID 和排除原因，避免重复。
3. 如果要操作网站，按机器上的网站数据工作约定先发现可用 CLI（通常先运行 `opencli list -f yaml`），再使用已登录的浏览器会话。优先使用可读的浏览器/电脑控制能力；不要要求用户在聊天里提供账号、密码、Cookie 或验证码。
4. 若页面未登录、结果不可读或触发风控，停止盲试，告知用户需要在浏览器中完成登录/接管；登录恢复后继续，不把凭证写入文件。

## 搜索方法

### 1. 按岗位建立搜索矩阵

每个新岗位都从当前 JD 重新提取搜索轴，不把上一岗位的公司清单或关键词清单当默认输入：

- **工作对象**：候选人实际负责的产品、用户、客户、内容形态、技术/业务模块或组织问题。
- **职责与结果**：JD 中的动词、交付物、业务指标、管理范围和协作对象；为每项职责准备同义词、上下游词和职位词。
- **渠道/工具/方法**：只从当前岗位的工作内容推导平台、系统、方法和专业术语。比如 JD 明确涉及投放或种草时才扩展相应工具词；不能因为上一单用过某工具，就把它带入本案。
- **行业/场景**：根据当前岗位的产品、人群、客户和业务模式确定同类及可迁移场景；不要把某一单的母婴、儿童或其他行业范围固化成通用范围。
- **公司来源轴**：根据当前岗位推导应搜的公司类型和公司集合，例如产品/品牌方、平台/软件方、内容或服务方、渠道方、机构或出版方等，仅在 JD 证据支持时启用，不强制套用甲方/乙方/DP 分类。

先用当前 JD 的高信号职责和业务对象搜索，再按当前岗位推导出的渠道/工具、同类场景、公司集合和岗位族逐层扩展。每条查询都记录推导依据、结果数、页数、命中公司和重复情况。

如果 JD 无法推导出足够区分度的关键词或公司范围，先用已有 JD 信息做一轮基础搜索，并向用户询问是否有参考关键词/公司；用户补充后只把相关内容加入本案，不自动变成其他岗位的默认词表。

### 1.5 召回与判定分离

搜索页的职责是发现候选人，候选人详情页的职责是作最终判断。搜索页筛选只使用稳定、低误伤、确实有助于检索效率的约束；会显著缩窄候选池、完整信息只在详情页展示，或容易与详情页不一致的条件，不得默认卡死在搜索条件中。

尤其是期望薪资、薪次数、实际求职意向、候选人级徽章和复杂经历证据：先保留召回，再在详情页逐份核验。用户明确要求使用某项搜索页筛选时，记录其可能造成的遗漏；必要时保留一条放宽筛选的补充搜索线。搜索结果卡、平台促销或列表标签都只能作线索，不能替代候选人详情页事实。

### 2. 公司反查

搜索过程中只要出现与当前岗位画像相关的公司，就记录到公司队列，并注明它为何相关：产品/客户/渠道/业务模式/岗位职能/候选人履历证据中的哪一项触发了它。不要用固定的甲方、乙方、DP 或行业名单替代这个判断。

对公司名逐个反查时，使用当前岗位重新推导的等价职能、业务对象、渠道和结果词，查看该公司内可能承载同类工作的岗位。公司反查用于找出“不写目标关键词但实际做过目标业务”的候选人；反查结果要去重。

每个入队公司必须记录：发现来源、与当前 JD 的关联依据、计划使用的“公司 + 等价职能 / 业务对象 / 结果词”组合、状态（待查 / 已查 / 重复 / 无结果）和新增候选数。优先将首轮高相关候选人履历中的同类公司、相邻公司、上下游、竞品或渠道伙伴转入队列；每轮反查后，把新发现的相关公司回灌队列。当连续反查只产生重复或不相关结果时，该分支才可停止。

允许跨岗位复用的是“候选人证据 → 发现相关公司 → 用岗位等价职能反查”的方法；上一岗位具体用过的公司名、关键词、工具名和行业名单，只有在与当前岗位工作内容相同或高度相似并完成相似性判断后才能复用。

### 3. 逐份核验

候选人卡片只用于发现；进入候选人详情页后，重新核验并记录：

- 姓名展示形式、年龄、当前城市/工作地、求职意向城市和当前状态。
- 最近公司/岗位及与 JD 最相关的 1–3 段经历。
- 可见的业务证据：品类、平台、内容/投放工具、GMV/用户/团队/项目结果和甲乙方交付关系。
- 薪资以详情页“求职意向/期望薪酬”原文为准；记录月薪区间下沿、上沿、薪次数及币种（如展示），不要只写一个“看起来合适”的数字。搜索结果卡和搜索页年薪筛选只能作发现线索，不可替代最终判断。
- 详情页链接和本次搜索来源。只记录完成判断所需的最小信息。

若搜索结果页与详情页在年龄、城市、意向、徽章或薪资等字段上不一致，以详情页为准，并记录冲突。公司与职位名称高度相关、但详情页未写足工作内容或结果的人，保留到待验证池，并明确缺失的证据；不因信息不足直接剔除，也不将其称为已匹配。

不要点击“立即沟通”“继续沟通”“查看联系方式”“获取联系方式”“索要附件”等入口；不要发送消息、下载/导出联系方式、建立外部联系或替用户承诺职位。除非用户另行明确授权并在临近外部动作时确认，否则本 skill 只负责搜索、核验和整理。

## 筛选与分层

- 用户明确给出的年龄、地域、薪资、到岗等条件视为筛选条件；没有给出的不要擅自补成硬条件。
- base 城市和候选人期望城市分开记录。候选人当前不在 base 不等于剔除，只要期望城市符合要求，就标记迁移/到岗风险。
- 先把用户的薪资表述翻译成可复核的规则：最低可接受薪资看区间下沿；预算上限看区间上沿；指定薪资带记录双方上下沿并计算交集；薪次数、提成、年终奖和固定月薪分开记录。用户未说明“下沿 / 上沿 / 总包”的，不擅自假定。
- 用户提供薪资区间时，候选人期望区间与岗位区间只要有交集即可保留，不要求候选人区间完全落在岗位区间内。按相同的月薪口径比较：交集有实际宽度则薪资匹配；仅在一个端点相接则标为薪资卡线/待确认；完全无交集才视为薪资不匹配。薪次数和总包差异作为后续确认风险，不在搜索阶段压缩召回。
- 推荐池：详情页业务证据足够，关键职责与筛选条件匹配。可沟通边界池：业务证据充分，但薪资仅卡线、城市迁移、状态或其他条件存在明确风险；写清差距。待验证池：公司、岗位名称或行业信号高度相关，但工作内容、结果、薪资或其他关键证据不足；写清待核验项，不计入已验证 shortlist。剔除池：已有明确反证，例如职责明显无关、薪资区间完全无交集或用户明确条件不符。
- 推荐排序优先考虑：核心业务结果 > JD 关键职责覆盖 > 内容/平台/工具深度 > 团队和跨部门能力 > 城市与薪资风险。不要因为公司名气替代业务证据。

## 交付

默认交付一份 Markdown 清单；用户要求“表格”“Excel”或后续需要反复筛选时，另建可筛选 `.xlsx`。推荐字段：

`候选人｜年龄｜当前城市｜期望城市｜最近公司/岗位｜来源类型｜核心相关经验｜内容/电商/投放关键词｜薪资原文｜薪资区间交集｜薪次数｜城市校验｜薪资校验｜综合判断｜猎聘链接`

输出中必须包含：搜索日期、搜索词/公司队列、筛选口径、推荐池、可沟通边界池、待验证池、剔除原因和未核实项。候选人的电话、邮箱、微信、精确住址、照片和其他不必要的敏感信息不进入输出。

## 案例迭代

用户在消息中用 `迭代：` 标出的内容，均视为用户要求更新这套 searching 工作流：先作用于本案；如果是可跨案例复用的规则，再同步到本 skill 的迭代记录。看到该标记时：

1. 把它解析为“新增、放宽、收紧、改排序或改交付”的明确变化，写入当前案 `case-config.md`，并在 `search-log.md` 记录日期、变化前后和受影响的搜索分支。
2. 只重跑受影响的查询和公司反查，保留已核验结果，做候选人去重；若迭代改变筛选条件，重新计算推荐池、可沟通边界池和待验证池。
3. 迭代完成后，将可复用的规则追加到本 skill 的 `references/iteration-log.md`，并保留原规则的日期和理由；案例特有的公司、薪资和人选只写入案例目录，不污染 skill。若用户明确说“只对本案生效”，则只更新案例文件。
4. 回报本次迭代实际改变了什么、增删了哪些候选人、哪些结果仍待核验。没有 `迭代：` 标记时，不要自行改写 skill 的通用规则。

详细的岗位自适应搜索矩阵、跨岗位复用边界、日志字段和迭代登记格式见 [references/search-playbook.md](references/search-playbook.md) 与 [references/iteration-log.md](references/iteration-log.md)。

## 完成检查

- 是否真的搜索了关键词线和公司反查线，且记录了查询/公司队列？
- 是否打开详情页核验了推荐池中的每个人？
- 年龄、地域、薪资是否按用户定义的口径逐项判定？
- 是否去重、标明边界与证据不足，而不是把所有搜索结果都叫“合适”？
- 是否只做了搜索/核验/整理，没有触达候选人或获取联系方式？
- 是否写入本案输出并完成记忆收尾；若用户要求表格，是否做了可筛选和基础视觉/数据校验？
